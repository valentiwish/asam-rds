############################################################
# .gitattributes（Spring Boot + Vue 前后端分离 推荐）
# 目标：
# 1) 文本文件自动识别并规范化行尾
# 2) 关键源码/配置强制 LF，避免跨平台（Windows/Linux/CI）换行冲突
# 3) 二进制文件明确标记，避免被当文本处理
############################################################

# 1) 自动识别文本文件，并在仓库存储为 LF（基础必备）
* text=auto

############################################################
# 2) 关键文本文件：强制使用 LF（强烈建议）
#    原因：后端常部署在 Linux、CI 也多是 Linux；
#          Vue/Node 工具链、Shell 脚本对 CRLF 更敏感；
#          yml/properties 等配置文件 CRLF/LF 混用也容易产生“全文件变化”的 diff
############################################################

# 后端：Java / 配置 / 构建脚本
*.java           text eol=lf
*.kt             text eol=lf
*.xml            text eol=lf
*.properties     text eol=lf
*.yml            text eol=lf
*.yaml           text eol=lf
*.gradle         text eol=lf
*.groovy         text eol=lf
pom.xml          text eol=lf
mvnw             text eol=lf
gradlew          text eol=lf

# 前端：Vue / JS / TS / 样式 / 页面
*.vue            text eol=lf
*.js             text eol=lf
*.ts             text eol=lf
*.jsx            text eol=lf
*.tsx            text eol=lf
*.css            text eol=lf
*.scss           text eol=lf
*.less           text eol=lf
*.html           text eol=lf
*.json           text eol=lf

# 文档 & 配置
*.md             text eol=lf
*.txt            text eol=lf
*.ini            text eol=lf
*.conf           text eol=lf
*.env            text eol=lf
*.editorconfig   text eol=lf

# 脚本：Linux/CI 常用（非常建议）
*.sh             text eol=lf

############################################################
# 3) Windows 脚本：建议保持 CRLF（可选）
#    如果你们有人会直接双击运行 bat/cmd，CRLF 兼容性更好
############################################################
*.bat            text eol=crlf
*.cmd            text eol=crlf

############################################################
# 4) 二进制文件：明确标记 binary
#    原因：避免 Git 把它们当文本做换行转换/差异比较，导致文件损坏或无意义 diff
############################################################
*.png            binary
*.jpg            binary
*.jpeg           binary
*.gif            binary
*.bmp            binary
*.ico            binary
*.psd            binary

*.pdf            binary
*.zip            binary
*.7z             binary
*.rar            binary

*.jar            binary
*.war            binary
*.ear            binary

# 前端字体等资源（常见）
*.woff           binary
*.woff2          binary
*.ttf            binary
*.eot            binary
